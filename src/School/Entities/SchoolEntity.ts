export class School {
  name: string;
  address: string;
  city: string;

  constructor(name: string, address: string, city: string) {
    this.name = name;
    this.address = address;
    this.city = city;
  }
}
